<!-- 新規フォームのとき -->
<!-- <form action="/blogs" method="post"></form> -->

<!-- 編集フォームのとき -->
<!-- <form action="/blogs/:id" method="post">
  <input type="hidden" method="patch">
</form> -->
<%= form_for(@blog) do |f| %>
  <% if @blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blog.errors.count, "error") %> prohibited this blog from being saved:</h2>

      <ul>
      <% @blog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <!-- <input type="text" name="blog[title]" value="#{blog.title}" id="blog_title"> -->
  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <!-- <textarea name="blog[content]" value="#{blog.content}" id="blog_content"> -->
  <div class="field">
    <%= f.label :content %>
    <%= f.text_area :content %>
  </div>

  <!-- <input type="submit" name="commit"> -->
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
